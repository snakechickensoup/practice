"use client";

import { useSuspenseQuery } from "@apollo/client";
import { CHARACTERS_QUERY } from "@/graphql/queries/characters";

const Page = () => {
  return <main></main>;
};

export default Page;
